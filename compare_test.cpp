#include<iostream>
using namespace std;
int main()
{
    string s1,s2;
    cin>>s1;
    cin>>s2;
    if(!s1.compare(s2))
        cout<<s1.compare(s2);
    else
        cout<<s1.compare(s2);

    return 0;
}
